# Solo para desarrollo local: spring-boot-docker-compose lo levanta al ejecutar ./mvnw spring-boot:run
services:
  postgres:
    image: 'postgres:17'
    environment:
      - 'POSTGRES_DB=mydatabase'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=myuser'
    ports:
      - '5432:5432'
